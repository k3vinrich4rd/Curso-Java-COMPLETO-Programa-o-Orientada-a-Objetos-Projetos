# Camada `dao`

Esta pasta define os contratos de acesso a dados da aplicação.

## Interfaces do módulo

- `SellerDao`
- `DepartmentDao`
- `DaoFactory`

## Papel da pasta

As interfaces descrevem as operações esperadas para cada entidade, enquanto a `DaoFactory` centraliza a criação das implementações JDBC.

